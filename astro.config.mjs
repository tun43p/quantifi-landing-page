import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  site: "https://quantifi-landing-page.github.io",
  // base: "https://github.com/tun43p/quantifi-landing-page",
});
